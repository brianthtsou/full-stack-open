{
  "name": "part4",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "run-test": "cross-env NODE_ENV=test node --test",
    "run-test-only": "cross-env NODE_ENV=test node --test --test-only",
    "test": "cross-env NODE_ENV=test node index.js",
    "main": "cross-env NODE_ENV=production node index.js",
    "dev": "cross-env NODE_ENV=development nodemon index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mongoose": "^8.3.2"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "jsonwebtoken": "^9.0.2",
    "nodemon": "^3.1.0",
    "supertest": "^7.0.0"
  }
}
